name: Build Production ERPNext Image

on:
  workflow_dispatch:
    inputs:
      image_tag:
        description: "Image tag to publish"
        required: true
        default: "v1"

permissions:
  contents: read
  packages: write

jobs:
  build:
    runs-on: ubuntu-24.04

    steps:
      - name: Checkout frappe_docker
        uses: actions/checkout@v4
        with:
          repository: frappe/frappe_docker
          ref: main
          path: frappe_docker

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Create apps.json
        run: |
          cat > apps.json <<'EOF'
          [
            {
              "url": "https://github.com/frappe/erpnext",
              "branch": "version-15"
            },
            {
              "url": "https://github.com/frappe/hrms",
              "branch": "version-15"
            },
            {
              "url": "https://github.com/CM-Torrejos/cardmasters_app",
              "branch": "v1.0.0"
            },
            {
              "url": "https://github.com/frappe/insights",
              "branch": "version-3"
            },
            {
              "url": "https://github.com/The-Commit-Company/raven",
              "branch": "main"
            }
          ]
          EOF

      - name: Encode apps.json
        run: |
          echo "APPS_JSON_BASE64=$(base64 -w 0 apps.json)" >> "$GITHUB_ENV"

      - name: Build and push image
        uses: docker/build-push-action@v6
        with:
          context: ./frappe_docker
          file: ./frappe_docker/images/custom/Containerfile
          push: true
          tags: ghcr.io/cm-torrejos/marcommax-production:${{ inputs.image_tag }}
          build-args: |
            FRAPPE_PATH=https://github.com/frappe/frappe
            FRAPPE_BRANCH=version-15
            PYTHON_VERSION=3.11.9
            NODE_VERSION=18.20.2
            APPS_JSON_BASE64=${{ env.APPS_JSON_BASE64 }}

      - name: Show image
        run: |
          echo "Published image:"
          echo "ghcr.io/cm-torrejos/marcommax-production:${{ inputs.image_tag }}"
